# AUTO-GENERATED — Work Item: 87 | Standards applied: WI-87, TESTING-PYTEST, PYTHON-3.11, SAFE-DEFAULTS
# Review before merging. Do not remove this header until reviewed.

[project]
name = "rag-knowledge-retrieval-poc"
version = "0.1.0"
description = "POC RAG-based knowledge retrieval API (FastAPI)"
requires-python = ">=3.11"
readme = "README.md"
dependencies = [
  "fastapi>=0.110",
  "uvicorn>=0.23",
  "pydantic>=2.6",
  "pydantic-settings>=2.2",
]

[project.optional-dependencies]
test = [
  "pytest>=8.0",
  "httpx>=0.27",
]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
